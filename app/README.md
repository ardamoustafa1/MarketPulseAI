# Legacy Folder Notice

This `app/` directory is intentionally kept as a legacy placeholder and is not used by runtime services.

Active application code lives under:

- `apps/api`
- `apps/admin`
- `apps/mobile`

Do not add new product code under this directory.
